{% extends 'base.html.twig' %}

{% block title %}RWAR!{% endblock %}

{% block body %}
    <div id="subreddit-name">
        <select id="subreddit" style="margin-top: 20px">
            {% for subreddit in subreddits %}
                <option
                        value="{{ subreddit.id }}"
                        {% if subreddit.name == wallpaper.subreddit.name %}
                    selected="selected"
                        {% endif %}>
                    /r/{{ subreddit.name }}
                </option>
            {% endfor %}
        </select>
        ({{ subreddit_num_unrated }} unrated)
    </div>

    <div style="margin: auto; margin-top: -25px; text-align: center">
        <div style="text-align: center">
            <a id="link-favourite" href="{{ url('wallpaper_favourite', {id: wallpaper.id, subredditId: wallpaper.subreddit.id}) }}">Favourite</a>
            |
            <a id="link-reject" href="{{ url('wallpaper_reject', {id: wallpaper.id, subredditId: wallpaper.subreddit.id}) }}">Reject</a>
            |
            <a id="link-set" href="{{ url('wallpaper_set', {id: wallpaper.id, subredditId: wallpaper.subreddit.id}) }}">Set</a>
        </div>
        <img alt="wallpaper" id="wallpaper-image" src="{{ wallpaper.imageUrl }}" style="width: 100%; padding: 10px;"/>
        <div id="image-resolution"></div>
    </div>
{% endblock %}
